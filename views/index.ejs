<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title%></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css">

    <link rel="stylesheet" href="/static/style.css">
</head>
<body>
   <div id="login" class="text-center center-div border-4">
    <div class="container w-25 border py-5">
        <div class="title">
            <h2 class="font-weight-bold text-dark bg-gradient pt-2 pb-2">
                LOGIN SYSTEM
            </h2>
            <span class="bg-span">Login in for the existing users</span>
            <% if(locals.logout){%>
                <div class="alter alert-success text-success text-center">
                    <%=logout%>
                </div>
                <%}%>

        </div>

        <form action="/route/login" method="POST" class="pt-3">
            <div class="form-group">
                <input type="email" class="form-control" name="email" placeholder="Enter your email">
                <small class="form-text text-left text-dark"> Register email address</small>

            </div>
            <div class="form-group">
                <input type="password" name="password" placeholder="password" class="form-control">
            </div>
            <button type="submit" class="btn p-3 m-4 w-50 btn-success rounded-pill">Submit</button>
        </form>
    </div>
   </div> 
</body>
</html>